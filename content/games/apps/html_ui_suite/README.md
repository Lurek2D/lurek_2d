# HTML UI Suite

**Category:** apps / HTML UI  
**Status:** skeleton

Single HTML/CSS app replacing separate HUD, inventory, dialog, settings, scoreboard, and loadDocument demos.

**Modules:** `html`, `render`, `input`, `filesystem`

Next steps:
- Add tabs for HUD, inventory, dialog, settings, and scoreboard.
- Route mouse and keyboard input into the HTML document.
- Load one panel from local files to keep `loadDocument` covered.

Run:

```bash
cargo run -- content/games/apps/html_ui_suite
```
