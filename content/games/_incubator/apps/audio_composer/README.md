# Audio Composer

**Category:** apps / audio  
**Status:** skeleton

Upgrade target for music_composer. The app should become a piano roll, beat clock, MIDI, and DSP workbench.

**Modules:** `audio`, `midi`, `dsp`, `ui`

Next steps:
- Add note grid editing and playback.
- Add waveform/spectrum views.
- Add simple lowpass/highpass/gain controls.

Run:

```bash
cargo run -- content/games/apps/audio_composer
```
