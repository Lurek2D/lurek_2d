# Image Workbench

**Category:** apps / image processing  
**Status:** skeleton

Non-game application for image buffers, filters, compute kernels, diffing, and PNG export.

**Modules:** `image`, `effect`, `compute`, `filesystem`

Next steps:
- Add generated image fixtures and load/save controls.
- Add filter stack and before/after preview.
- Export images through the filesystem API.

Run:

```bash
cargo run -- content/games/apps/image_workbench
```
