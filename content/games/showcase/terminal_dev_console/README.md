# Terminal Dev Console

**Category:** showcase / developer tools  
**Status:** skeleton

Upgrade target for terminal_demo with devtools, debugbridge, and log integration.

**Modules:** `terminal`, `devtools`, `debugbridge`, `log`

Next steps:
- Add command input, frame stats, log history, and inspector panel.
- Poll debugbridge events without requiring external tools.
- Keep it useful as a real debugging surface.

Run:

```bash
cargo run -- content/games/showcase/terminal_dev_console
```
