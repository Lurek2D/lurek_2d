name = "extension"
description = "Extension role for extension/ and VS Code integration work."
model = "gpt-5.2-codex"
model_reasoning_effort = "medium"
approval_policy = "on-failure"
sandbox_mode = "workspace-write"
personality = "pragmatic"
