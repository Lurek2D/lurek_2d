name = "builder"
description = "Builder role for tools/ scripts, packaging, CI, and release workflow work."
model = "gpt-5.2-codex"
model_reasoning_effort = "medium"
approval_policy = "on-failure"
sandbox_mode = "workspace-write"
personality = "pragmatic"
