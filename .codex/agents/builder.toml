# Use when the change is about automation rather than product behavior.
# Typical work: tools/, build profiles, CI, release packaging, audit scripts, repository automation.
# Best fit skills: create-build, create-tool.
# Preferred tools: shell, Python/PowerShell scripts, CI/build commands, audit tooling.
# Do not use for gameplay, docs semantics, or core runtime implementation.
name = "builder"
description = "Build engineer for tools/, CI, packaging, automation, and developer-facing CLI scripts."
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
model_reasoning_summary = "concise"
model_verbosity = "low"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
personality = "pragmatic"
service_tier = "default"
web_search = "disabled"
