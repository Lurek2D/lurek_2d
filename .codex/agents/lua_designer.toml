# Use when the task is about the public Rust<->Lua boundary.
# Typical work: src/lua_api/, wrapper thinness, API parity, argument naming, return shapes, scripting ergonomics.
# Best fit skills: review-api, create-module when the hard part is the binding surface.
# Preferred tools: shell, repo MCP, audits for wrapper thinness and coverage.
# Choose this instead of developer when the hard part is API surface quality rather than engine internals.
name = "lua_designer"
description = "Lua API designer for src/lua_api/ bindings, wrapper thinness, and public scripting ergonomics."
model = "gpt-5.4"
model_reasoning_effort = "high"
model_reasoning_summary = "auto"
model_verbosity = "medium"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
personality = "pragmatic"
service_tier = "default"
web_search = "disabled"
