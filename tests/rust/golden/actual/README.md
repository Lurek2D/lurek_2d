# tests/rust/golden/actual — Generated Test Outputs

Written on every test run for inspection. **Gitignored** — do not commit.

If a golden test fails, compare `actual/<name>` against `expected/<name>` to see the diff.
