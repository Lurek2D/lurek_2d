# tests/rust/golden/screenshots — Visual Evidence PNGs

CPU-rendered PNG screenshots captured by `save_test_screenshot()`. Committed to the repository as visual evidence of the engine's pixel-level output.

Screenshots are **not** compared automatically — regenerate by re-running `cargo test --test golden_tests`.
