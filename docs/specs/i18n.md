# i18n

## TL;DR

- Manages translation catalogs, plural rules, and locale-aware formatting.

## General Info

- Module group: `Feature Systems`
- Source path: `src/i18n/`
- Binding: `src/lua_api/i18n_api.rs`
- Namespace: `lurek.i18n`
- Lua API surface: `36` functions, `2` types, `0` methods
- Rust test path(s): tests/rust/unit/i18n_tests.rs
- Lua test path(s): tests/lua/unit/test_i18n_unit.lua

## Summary

- This module gives users runtime localization support for translated text, plural forms, and locale-aware formatting.
- Translation catalogs can be loaded from Lua tables, JSON, or TOML and queried through one lookup path.
- Fallback locale chains help keep UI functional when specific keys are missing.
- Placeholder interpolation supports dynamic text composition with runtime values.
- Pluralization helpers handle language-specific quantity forms for more natural phrasing.
- Number and date formatting APIs localize separators and date styles automatically.
- Locale switching can happen during runtime, enabling live language changes in settings screens.
- Coverage and search helpers support localization QA and translation completeness checks.
- For users, this module centralizes language behavior instead of scattering string rules across scripts.
- The practical result is more maintainable multilingual UX with fewer edge-case formatting bugs.

This module is mostly self-contained inside the Feature Systems group. Cross-module behavior should stay in the referenced Rust source files and Lua bindings rather than being duplicated here.

## Imports

- No top-level `crate::<module>` imports were detected in this module's Rust source files.

## Files

### catalog.rs

- Provides a locale-first translation catalog that serves runtime string lookup with fallback chains.
- Stores per-locale key tables and active language state so content can switch language without rebuild.
- Performs resilient key resolution that walks configured fallback order before reporting missing entries.
- Supports import flattening from TOML and JSON trees into dot-path lookup keys for uniform access.
- Computes coverage gaps across locales to expose untranslated keys during QA and localization passes.
- Offers category grouping and search-style scans that help tools inspect large translation inventories.
- Includes locale validation, RTL detection, and system-locale discovery for startup language selection.
- Keeps lightweight caches to accelerate repeated text and word lookups in hot UI translation paths.

### format.rs

- Formats locale-facing numbers and dates into human-readable strings for UI presentation.
- Chooses decimal and thousands separators from locale prefixes to match regional reading habits.
- Converts epoch-based day counts and timestamps into calendar components and formatted date output.
- Supplies ISO, short, and long date renderings for compact HUD and verbose panel contexts.
- Encodes month-name mapping logic used by textual date labels in localized interfaces.

### interpolation.rs

- Expands localized template text by replacing `{name}` placeholders with runtime values.
- Preserves unknown tokens and supports escaped braces so authored text stays readable under partial data.
- Provides both map-driven and pair-list substitution flows for ergonomic caller integration.
- Keeps output assembly linear and allocation-light for frequent UI string rendering.

### mod.rs

- High-level i18n module entry point combining catalogs, formatting, interpolation, and plural rules.
- Re-exports locale utilities and core types so callers consume localization features from one surface.
- Defines the integration spine for language detection, lookup, and human-friendly output shaping.

### plural.rs

- Encodes plural category logic used to choose grammatically correct translation variants by count.
- Maps quantity values to form keys with dedicated paths for English-style and Slavic-style rules.
- Supports stable key conversion so plural forms align with translation-table naming conventions.
- Provides fallback-aware pluralized string selection when exact form variants are absent.



## Lua API Ref

### Functions

- `lurek.i18n.buildIndex() -> table`: Builds a word-to-keys search index from the catalog.
- `lurek.i18n.categories() -> string[]`: Returns top-level translation key categories.
- `lurek.i18n.detectLocale() -> string`: Detects the system locale when available.
- `lurek.i18n.formatDate(timestamp, fmt?) -> string`: Formats a timestamp with the active locale and a named format.
- `lurek.i18n.formatNumber(n, opts?) -> string`: Formats a number with locale-aware separators and optional decimal precision.
- `lurek.i18n.getAvailableLanguages() -> string[]`: Returns sorted locale codes currently loaded in the catalog.
- `lurek.i18n.getBase() -> string`: Returns the base locale string stored by the localization module.
- `lurek.i18n.getFallbacks() -> string[]`: Returns fallback locale codes in lookup order.
- `lurek.i18n.getKeys() -> string[]`: Returns sorted translation keys known to the catalog.
- `lurek.i18n.getLanguage() -> string`: Returns the active locale code string.
- `lurek.i18n.getLanguages() -> string[]`: Returns sorted locale codes currently loaded in the catalog.
- `lurek.i18n.getLoadedLocales() -> string[]`: Returns locale codes currently loaded in the catalog.
- `lurek.i18n.hasKey(key) -> boolean`: Returns whether the catalog contains a translation key in active or fallback locales.
- `lurek.i18n.hasLanguage(locale) -> boolean`: Returns whether a locale has translations loaded.
- `lurek.i18n.interpolate(template, vars) -> string`: Replaces `{name}` placeholders in a template using string variables.
- `lurek.i18n.isRTL(locale?) -> boolean`: Returns whether a locale is written right-to-left.
- `lurek.i18n.keyCount() -> integer`: Returns the number of translation keys known to the catalog.
- `lurek.i18n.keysInCategory(category) -> string[]`: Returns translation keys belonging to one category prefix.
- `lurek.i18n.loadString(locale, content, format) -> nil`: Loads translations for a locale from TOML or JSON source text.
- `lurek.i18n.loadTable(locale, tbl) -> nil`: Loads translations for a locale from a nested Lua table flattened with dot-separated keys.
- `lurek.i18n.localeCoverage(reference) -> table`: Returns missing translation keys for all locales compared to a reference locale.
- `lurek.i18n.mergeLocale(locale, entries) -> nil`: Merges flat translation entries into an existing locale.
- `lurek.i18n.offChange() -> nil`: Removes every registered locale-change callback.
- `lurek.i18n.onChange(cb) -> nil`: Registers a locale-change callback using the shorter alias name.
- `lurek.i18n.onLanguageChange(cb) -> nil`: Registers a callback invoked when the active locale changes.
- `lurek.i18n.pluralFor(n) -> string`: Returns the English plural category key for a number.
- `lurek.i18n.search(query, limit?) -> table`: Searches translation keys and values for a query string.
- `lurek.i18n.searchIndexed(index, query, limit?) -> string[]`: Searches a prebuilt word index and returns matching keys.
- `lurek.i18n.setBase(locale) -> nil`: Sets the base locale string stored by the localization module.
- `lurek.i18n.setFallbacks(locales) -> nil`: Replaces the fallback locale list used for missing translations.
- `lurek.i18n.setKey(locale, key, value) -> nil`: Sets one translation value for one locale and key.
- `lurek.i18n.setLanguage(locale) -> nil`: Sets the active locale and invokes registered change callbacks with new and old locale values.
- `lurek.i18n.t(key, vars?, count?) -> string`: Translates a key using the active locale, optional variables, and optional English plural selection.
- `lurek.i18n.tGender(key, gender, vars?) -> string`: Translates a gender-specific key variant when present, then falls back to the base key.
- `lurek.i18n.unloadTable(locale) -> boolean`: Removes all translations for a locale from the catalog.
- `lurek.i18n.validateLocale(locale) -> boolean`: Returns whether a locale code has a valid syntax.

### Callbacks

- `lurek.i18n.onChange` param `cb` (`function`): Callback receiving `(new_locale, old_locale)`.
- `lurek.i18n.onLanguageChange` param `cb` (`function`): Callback receiving `(new_locale, old_locale)`.

### Enums

- No documented module-level enums/constants.

### Types

#### LI18nLocaleCoverageResult Type

- Generated result shape from @field tags.

##### Fields

- `key` (`string`): Translation key.
- `missing_in` (`string[]`): Locales missing this key.

##### Methods

- No documented methods.

#### LI18nSearchResult Type

- Generated result shape from @field tags.

##### Fields

- `key` (`string`): Translation key.
- `value` (`string`): Translated value.

##### Methods

- No documented methods.

## References

- No top-level `crate::<module>` imports were detected in this module's Rust source files.

## Notes

- No additional module-specific notes.
