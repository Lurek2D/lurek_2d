# Asset-API

> This page is a stable audit entry point. Generated module details live in [Module-asset.md](Module-asset.md).

## `lurek.asset`

Ref-counted asset registry with typed load/get paths, metadata (`name`, `group`, `tags`), and lookup helpers (`findByName`, `findByGroup`, `findByTag`, `findByType`).

For the canonical module contract, see [asset.md](../specs/asset.md).
