<!-- Auto-generated by tools/docs/gen_wiki.py; do not edit by hand. -->

# Module grep

## Navigation

[Home](Home) | [Modules](Modules) | [API](API) | [Examples](Examples) | [Reference Games](Reference-Games) | [Lureksome](Lureksome)

## Table of Contents

- [🎯 Purpose](#purpose)
- [📋 Summary](#summary)
- [📁 Source Files](#source-files)
- [🧩 Key Types](#key-types)
- [📖 API Overview](#api-overview)
- [⚙️ Module Functions](#module-functions)
  - [Module-Level Functions](#module-level-functions)
- [🔷 Module Types](#module-types)
  - [LFileFilter](#lfilefilter)
  - [LGrepEngine](#lgrepengine)
- [🔹 Module Methods](#module-methods)
  - [LFileFilter Methods](#lfilefilter-methods)
  - [LGrepEngine Methods](#lgrepengine-methods)
- [💡 Examples](#examples)
- [🎮 Reference Games](#reference-games)
- [🔗 Related Modules](#related-modules)

This page is generated from the current module specs, examples, and Lua API data.

**Module group:** Feature Systems
**Namespace:** `lurek.grep`

## 🎯 Purpose

Text search engine for game content: literal, regex, glob, fuzzy, parallel file search.

[⬆ back to top](#table-of-contents)

## 📋 Summary

Text search engine for game content: literal, regex, glob, fuzzy, parallel file search.

[⬆ back to top](#table-of-contents)

## 📁 Source Files

No source-file descriptions were found in the module spec.

[⬆ back to top](#table-of-contents)

## 🧩 Key Types

- `LFileFilter` (4 methods) - Lua userdata that controls which files are scanned by a LGrepEngine instance.
- `LGrepEngine` (5 methods) - Lua userdata that performs pattern-based search across game content files.

[⬆ back to top](#table-of-contents)

## 📖 API Overview

- Source spec: [docs/specs/grep.md](../blob/main/docs/specs/grep.md)
- Module-level functions: 7
- Lua-visible types: 2
- Total type methods: 9


[⬆ back to top](#table-of-contents)

## ⚙️ Module Functions

### Module-Level Functions

#### lurek.grep.jsonSearch

#### Definition

```lua
--- Search JSON files under a path for a literal pattern and return a result table.
---@param path string Root directory to search.
---@param pattern string Literal string to find.
---@return table Table of search matches found in JSON files.
lurek.grep.jsonSearch = function(path, pattern) end
```

#### Description

Search JSON files under a path for a literal pattern and return a result table.

Parameters:

- `path` (`string`, required): Root directory to search.
- `pattern` (`string`, required): Literal string to find.

Returns: - Table of search matches found in JSON files.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local results = lurek.grep.jsonSearch("content/examples", "api-stub")
    print("json results = " .. #results)
end
```

#### lurek.grep.logSearch

#### Definition

```lua
--- Search engine log files for a pattern and return structured result entries.
---@param pattern string Literal string to find in log files.
---@return table Log entries matching the pattern as a result table.
lurek.grep.logSearch = function(pattern) end
```

#### Description

Search engine log files for a pattern and return structured result entries.

Parameters:

- `pattern` (`string`, required): Literal string to find in log files.

Returns: - Log entries matching the pattern as a result table.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local results = lurek.grep.logSearch("logs/runtime.log", "ERROR", "panic")
    print("log results = " .. #results)
end
```

#### lurek.grep.luaFilter

#### Definition

```lua
--- Create an LFileFilter pre-configured to match only Lua source files.
---@return LuaValue A new LFileFilter configured for Lua source files.
lurek.grep.luaFilter = function() end
```

#### Description

Create an LFileFilter pre-configured to match only Lua source files.

Returns: - A new LFileFilter configured for Lua source files.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local fil = lurek.grep.luaFilter()
    print("lua filter created = " .. tostring(fil ~= nil))
end
```

#### lurek.grep.newEngine

#### Definition

```lua
--- Create a new LGrepEngine with default file-type filters for game content.
---@return LuaValue A new LGrepEngine instance with default filters.
lurek.grep.newEngine = function() end
```

#### Description

Create a new LGrepEngine with default file-type filters for game content.

Returns: - A new LGrepEngine instance with default filters.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local eng = lurek.grep.newEngine()
    print("engine created = " .. tostring(eng ~= nil))
end
```

#### lurek.grep.newEngineOpts

#### Definition

```lua
--- Create a new LGrepEngine with a custom LFileFilter controlling which files are scanned.
---@param filter LFileFilter File filter to use for this engine instance.
---@return LuaValue A new LGrepEngine instance with the custom filter.
lurek.grep.newEngineOpts = function(filter) end
```

#### Description

Create a new LGrepEngine with a custom LFileFilter controlling which files are scanned.

Parameters:

- `filter` (`LFileFilter`, required): File filter to use for this engine instance.

Returns: - A new LGrepEngine instance with the custom filter.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local opts = { case_sensitive = false, threads = 2, whole_word = false }
    local eng = lurek.grep.newEngineOpts(opts)
    print("engine with opts created = " .. tostring(eng ~= nil))
end
```

#### lurek.grep.newFilter

#### Definition

```lua
--- Create a new LFileFilter with default extension-based inclusion rules.
---@return LuaValue A new LFileFilter with default extension rules.
lurek.grep.newFilter = function() end
```

#### Description

Create a new LFileFilter with default extension-based inclusion rules.

Returns: - A new LFileFilter with default extension rules.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local fil = lurek.grep.newFilter()
    fil:addExtension("lua")
    print("filter created = " .. tostring(fil ~= nil))
end
```

#### lurek.grep.search

#### Definition

```lua
--- Search a directory tree for a literal text pattern; return a result table.
---@param path string Root directory to search.
---@param pattern string Literal string to find.
---@return table Table of search matches found in the directory.
lurek.grep.search = function(path, pattern) end
```

#### Description

Search a directory tree for a literal text pattern; return a result table.

Parameters:

- `path` (`string`, required): Root directory to search.
- `pattern` (`string`, required): Literal string to find.

Returns: - Table of search matches found in the directory.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local results = lurek.grep.search("content/examples", "api-stub")
    print("files searched = " .. results.files_searched)
    print("total matches = " .. results.total_matches)
end
```


[⬆ back to top](#table-of-contents)

## 🔷 Module Types

### LFileFilter

#### Description

Lua userdata that controls which files are scanned by a LGrepEngine instance.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
--
-- Topics: grep engine, file filters, search, JSON search, log search.
-- ==========================================================================

-- Quick search (simplest usage)
--@api-stub: lurek.grep.newEngine
do
    local eng = lurek.grep.newEngine()
    print("engine created = " .. tostring(eng ~= nil))
end

--@api-stub: lurek.grep.newEngineOpts
do
    local opts = { case_sensitive = false, threads = 2, whole_word = false }
    local eng = lurek.grep.newEngineOpts(opts)
    print("engine with opts created = " .. tostring(eng ~= nil))
end

--@api-stub: lurek.grep.newFilter
do
    local fil = lurek.grep.newFilter()
    fil:addExtension("lua")
    print("filter created = " .. tostring(fil ~= nil))
end

--@api-stub: lurek.grep.luaFilter
do
    local fil = lurek.grep.luaFilter()
    print("lua filter created = " .. tostring(fil ~= nil))
end

--@api-stub: lurek.grep.search
do
    local results = lurek.grep.search("content/examples", "api-stub")
    print("files searched = " .. results.files_searched)
    print("total matches = " .. results.total_matches)
end

--@api-stub: lurek.grep.jsonSearch
do
    local results = lurek.grep.jsonSearch("content/examples", "api-stub")
    print("json results = " .. #results)
end

--@api-stub: lurek.grep.logSearch
do
    local results = lurek.grep.logSearch("logs/runtime.log", "ERROR", "panic")
    print("log results = " .. #results)
```

### LGrepEngine

#### Description

Lua userdata that performs pattern-based search across game content files.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
--
-- Topics: grep engine, file filters, search, JSON search, log search.
-- ==========================================================================

-- Quick search (simplest usage)
--@api-stub: lurek.grep.newEngine
do
    local eng = lurek.grep.newEngine()
    print("engine created = " .. tostring(eng ~= nil))
end

--@api-stub: lurek.grep.newEngineOpts
do
    local opts = { case_sensitive = false, threads = 2, whole_word = false }
    local eng = lurek.grep.newEngineOpts(opts)
    print("engine with opts created = " .. tostring(eng ~= nil))
end

--@api-stub: lurek.grep.newFilter
do
    local fil = lurek.grep.newFilter()
    fil:addExtension("lua")
    print("filter created = " .. tostring(fil ~= nil))
end

--@api-stub: lurek.grep.luaFilter
do
    local fil = lurek.grep.luaFilter()
    print("lua filter created = " .. tostring(fil ~= nil))
end

--@api-stub: lurek.grep.search
do
    local results = lurek.grep.search("content/examples", "api-stub")
    print("files searched = " .. results.files_searched)
    print("total matches = " .. results.total_matches)
end

--@api-stub: lurek.grep.jsonSearch
do
    local results = lurek.grep.jsonSearch("content/examples", "api-stub")
    print("json results = " .. #results)
end

--@api-stub: lurek.grep.logSearch
do
    local results = lurek.grep.logSearch("logs/runtime.log", "ERROR", "panic")
    print("log results = " .. #results)
```


[⬆ back to top](#table-of-contents)

## 🔹 Module Methods

### LFileFilter Methods

#### LFileFilter:addExtension

#### Definition

```lua
--- Add allowed file extensions — Lua userdata object exposed by the engine.
---@param ext string Extension (without dot).
function LFileFilter:addExtension(ext) end
```

#### Description

Add allowed file extensions — Lua userdata object exposed by the engine.

Parameters:

- `ext` (`string`, required): Extension (without dot).

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local fil = lurek.grep.newFilter()
    fil:addExtension("lua")
    fil:addExtension("toml")
    print("LFileFilter:addExtension ok")
end
```

#### LFileFilter:excludeExtension

#### Definition

```lua
--- Add excluded file extension for this object.
---@param ext string Extension to exclude.
function LFileFilter:excludeExtension(ext) end
```

#### Description

Add excluded file extension for this object.

Parameters:

- `ext` (`string`, required): Extension to exclude.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local fil = lurek.grep.newFilter()
    fil:excludeExtension("min.lua")
    print("LFileFilter:excludeExtension ok")
end
```

#### LFileFilter:excludePattern

#### Definition

```lua
--- Add path pattern to exclude for this object.
---@param pattern string Substring to exclude in file paths.
function LFileFilter:excludePattern(pattern) end
```

#### Description

Add path pattern to exclude for this object.

Parameters:

- `pattern` (`string`, required): Substring to exclude in file paths.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local fil = lurek.grep.newFilter()
    fil:excludePattern("test_")
    print("LFileFilter:excludePattern ok")
end
```

#### LFileFilter:setIncludeHidden

#### Definition

```lua
--- Set whether hidden files are included.
---@param include boolean Include hidden files.
function LFileFilter:setIncludeHidden(include) end
```

#### Description

Set whether hidden files are included.

Parameters:

- `include` (`boolean`, required): Include hidden files.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local fil = lurek.grep.newFilter()
    fil:setIncludeHidden(false)
    print("LFileFilter:setIncludeHidden ok")
end
```

### LGrepEngine Methods

#### LGrepEngine:count

#### Definition

```lua
--- Count total matches without returning line details.
---@param path string Directory to search.
---@param pattern string Text pattern.
---@return number Total match count.
function LGrepEngine:count(path, pattern) end
```

#### Description

Count total matches without returning line details.

Parameters:

- `path` (`string`, required): Directory to search.
- `pattern` (`string`, required): Text pattern.

Returns: `integer` - Total match count.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local eng = lurek.grep.newEngine()
    local n = eng:count("content/examples", "api-stub")
    print("LGrepEngine:count=" .. n)
end
```

#### LGrepEngine:multiSearch

#### Definition

```lua
--- Search with multiple patterns simultaneously.
---@param path string Directory to search.
---@param patterns table Array of literal patterns.
---@return table Search result.
function LGrepEngine:multiSearch(path, patterns) end
```

#### Description

Search with multiple patterns simultaneously.

Parameters:

- `path` (`string`, required): Directory to search.
- `patterns` (`table`, required): Array of literal patterns.

Returns: `table` - Search result.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local eng = lurek.grep.newEngine()
    local results = eng:multiSearch("content/examples", { "api-stub", "lurek.math" })
    print("LGrepEngine:multiSearch files=" .. results.files_searched)
    print("LGrepEngine:multiSearch matches=" .. results.total_matches)
end
```

#### LGrepEngine:search

#### Definition

```lua
--- Search a directory for a literal pattern.
---@param path string Directory to search.
---@param pattern string Text pattern to find.
---@return table Search result with matches, files_searched, total_matches, duration_ms.
function LGrepEngine:search(path, pattern) end
```

#### Description

Search a directory for a literal pattern.

Parameters:

- `path` (`string`, required): Directory to search.
- `pattern` (`string`, required): Text pattern to find.

Returns: `table` - Search result with matches, files_searched, total_matches, duration_ms.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local eng = lurek.grep.newEngine()
    local results = eng:search("content/examples", "api-stub")
    print("LGrepEngine:search files=" .. results.files_searched)
    print("LGrepEngine:search matches=" .. results.total_matches)
end
```

#### LGrepEngine:searchExt

#### Definition

```lua
--- Search with file extension filter.
---@param path string Directory to search.
---@param pattern string Text pattern.
---@param extensions table Array of file extensions (e.g., {"lua", "toml"}).
---@return table Search result.
function LGrepEngine:searchExt(path, pattern, extensions) end
```

#### Description

Search with file extension filter.

Parameters:

- `path` (`string`, required): Directory to search.
- `pattern` (`string`, required): Text pattern.
- `extensions` (`table`, required): Array of file extensions (e.g., {"lua", "toml"}).

Returns: `table` - Search result.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local eng = lurek.grep.newEngine()
    local results = eng:searchExt("content/examples", "api-stub", { "lua" })
    print("LGrepEngine:searchExt files=" .. results.files_searched)
    print("LGrepEngine:searchExt matches=" .. results.total_matches)
end
```

#### LGrepEngine:searchFiles

#### Definition

```lua
--- Search a specific provided list of files for text matches.
---@param files table Array of file paths.
---@param pattern string Text pattern.
---@return table Search result.
function LGrepEngine:searchFiles(files, pattern) end
```

#### Description

Search a specific provided list of files for text matches.

Parameters:

- `files` (`table`, required): Array of file paths.
- `pattern` (`string`, required): Text pattern.

Returns: `table` - Search result.

#### Example

Source: [grep.lua](../blob/main/content/examples/grep.lua)

```lua
do
    local eng = lurek.grep.newEngine()
    local results = eng:searchFiles({ "content/examples/grep.lua", "content/examples/font.lua" }, "api-stub")
    print("LGrepEngine:searchFiles files=" .. results.files_searched)
    print("LGrepEngine:searchFiles matches=" .. results.total_matches)
end
```


[⬆ back to top](#table-of-contents)

## 💡 Examples

- [grep.lua](../blob/main/content/examples/grep.lua) - API example

[⬆ back to top](#table-of-contents)

## 🎮 Reference Games

No direct references were found in `content/games/**/main.lua`.

[⬆ back to top](#table-of-contents)

## 🔗 Related Modules

- Previous: [globe](Module-globe)
- Next: [html](Module-html)
- [ai](Module-ai) - Game AI toolkit: FSMs, behaviour trees, GOAP, steering, utility AI, blackboards. Pure CPU.
- [animation](Module-animation) - Sprite animation: source-rect changes over time. Imports only math; headless-testable.
- [automation](Module-automation) - Automated input simulation for headless tests, QA replay, recorded sessions.
- [charts](Module-charts) - Software-rasterized chart renderers (line, bar, scatter, pie, area) to RGBA8 pixel buffers.
- [cursor](Module-cursor) - Cursor management: system cursors, custom images, animated cursors, trails, context switching, zoom.
- [dialog](Module-dialog) - Dialog/conversation engine: weighted topic/branch selection, state tracking, speaker registry.
