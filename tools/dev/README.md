# Lurek2D Dev Tools

> [!NOTE]
> Ten plik jest generowany automatycznie przez `tools/tests/gen_tool_registry.py`.

- **`parallel_cargo.py`**: Repository-owned cargo orchestration for build, run, test, lint, fmt, and doc.
- **`test_fix_loop.py`**: tools/scripts/test_fix_loop.py — Agent-friendly test-run / fix / re-run loop.
